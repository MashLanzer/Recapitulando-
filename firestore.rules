rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId}/series/{seriesId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    match /users/{userId}/calendar/{calendarId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
